<!DOCTYPE html>
<html>
<head>
  <title>Code Review Results</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { padding: 20px; }
    .container { max-width: 1000px; }
    .review-section { margin-bottom: 2rem; }
    .review-content { 
      background: #f8f9fa;
      padding: 1rem;
      border-radius: 4px;
      margin-top: 0.5rem;
    }
    pre {
      background: #f1f1f1;
      padding: 1rem;
      border-radius: 4px;
      overflow-x: auto;
    }
    code {
      background: #f1f1f1;
      padding: 0.2rem 0.4rem;
      border-radius: 3px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">Code Review Results</h1>
    
    <a href="/" class="btn btn-secondary mb-4">← Back to Review</a>

    <% @results.each do |file, review| %>
      <div class="review-section card">
        <div class="card-header">
          <h3 class="h5 mb-0"><%= file %></h3>
        </div>
        <div class="card-body">
          <div class="review-content">
            <%= @markdown.render(review) %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html> 